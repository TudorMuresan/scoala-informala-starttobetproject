var showApp = angular.module('showApp', [])

.controller('mainController', function($scope) {

  $scope.showFootball = false;
  if (!$scope.addMe) {return;}
  if( $scope.showFootball == false){
  	 $scope.
  }
  
});